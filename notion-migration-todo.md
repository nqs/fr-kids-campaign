# Notion Migration — Follow-up To-Do

Remaining content gaps found while rewiring cross-links across the 61 migrated
Notion pages. None of these are missing *links* — the underlying text was
dropped or never written during migration, so reconstructing it counts as new
content rather than link repair. Listed here for a deliberate follow-up pass.

- [x] **Party page** — restore the trailing clause "**S5 was played at Level 5;
      rebuild the table to L6 before a future session.** See party-dirk.md."
      (dropped entirely; needs the sentence rewritten plus a mention-page link
      to the Dirk character page)
- [x] **Geography page** — restore the "(see roster)" clause that originally
      followed Maelorn the Thorn Ward's name (dropped entirely; needs a
      mention-page link to Maelorn's roster entry)
- [x] **Roster page** — the folder-link sentence ("Each NPC now has their own
      page under `roster/`") was paraphrased into prose with no single
      identifiable target page; decide whether to rewrite it as a link to the
      Roster index or leave as prose — **decision:** left as-is; the Roster
      page already functions as the index in Notion's structure, so a
      self-link would be circular
- [x] **All 14 roster NPC subpages** — add a "← Back to NPC Roster" backlink
- [x] **All 7 party PC subpages** — add a "← Back to Party Roster" backlink
- [x] **All 6 session overview pages** — add a "← Session Index · Home" footer
- [x] **Session 5 overview page** — restore the second NOTE blockquote
      ("**Played.** See the session log...") that was dropped during migration
